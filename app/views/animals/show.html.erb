<div class="animal-show">

  <div class="card-animal-left">
    <%= cl_image_tag @animal.photo.key, height: 300, width: 400, crop: :fill %>
    <!-- <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @animal.photo.key, height: 300, crop: :fill %>')"></div>-->
    <div>
      <strong>chip:</strong><%= @animal.chip %>
      <strong>worm:</strong><%= @animal.worm %>
      <strong>vaccined:</strong><%= @animal.vaccined %>
    </div>
  </div>
  <div class="card-animal-right">
      <div class="card-animal-name">
        <h5>Name:<%= @animal.name %></h5>
      </div>
      <div class="card-animal-srg">
        <div><h5>Species:</h5><%= @animal.species %></div>
        <div><h5>Race:</h5><%= @animal.race %></div>
        <div><h5>Gender:</h5><%= @animal.gender %></div>
      </div>
      <div class="card-accesoiries">
        <h4>accessories:</h4>
        <p><%= @animal.accessories %></p>
      </div>
      <div class="card-animal-description">
        <h4>description:</h4>
        <p><%= @animal.description %></p>
        <strong>owner:</strong><%= @animal.owner.first_name %>
      </div>
  </div>
</div>
    <strong>daily_price:</strong><%= @animal.daily_price %>
